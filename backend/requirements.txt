# Backend dependencies for Smart Recruiter (Python 3.12 / FastAPI)
# Install with: pip install -r requirements.txt
# (preferably inside a virtual environment — see README.md → Quickstart)

# Web framework + ASGI server
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0

# Database / ORM
SQLAlchemy>=2.0,<3.0

# Settings + config
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0,<2.0

# Auth: JWT + password hashing
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7.4,<2.0
bcrypt<4.1   # passlib 1.7.4 is incompatible with bcrypt >= 4.1 (AttributeError on __about__)

# Form / file upload (required by FastAPI's OAuth2 / form parsers)
python-multipart>=0.0.9,<1.0

# Email-validator is used by pydantic EmailStr in the auth schemas
email-validator>=2.0,<3.0

# HTTP client (used by the Codewars integration)
httpx>=0.27,<1.0

# Tests
pytest>=8.0,<9.0
